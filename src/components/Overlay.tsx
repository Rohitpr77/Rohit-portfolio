"use client";

/**
 * Overlay.tsx — Alternative overlay approach (not used in production build)
 * Kept for reference. The overlay text sections are handled inside ScrollyCanvas.tsx.
 */

export default function Overlay() {
  return null;
}
